version: '3'

services:
  db:
    image: 'mysql:5.7'
    container_name: roberthuaman-api-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: toor
      MYSQL_DATABASE: robert_huaman_api
      MYSQL_USER: robert
      MYSQL_PASSWORD: robertapi
    ports:
      - ${API_DB_PORT}:3306
